{
    "builds": [
        {
            "src": "./app.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/hack/(.*)",
            "dest": "./app.py"
        }
    ]
}